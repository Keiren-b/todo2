for(let i = 0; i<localStorage.length; i++){
    console.log(JSON.parse(localStorage.getItem(keys[i])))}

    // begginer code for displaying all tasks/all projects. can use to dynamically create dom elements?